import Base from './Base';
import { TranslaterPoint } from '../translater';
import { EaseFn } from '@better-scroll/shared-utils';
export default class Animation extends Base {
    move(startPoint: TranslaterPoint, endPoint: TranslaterPoint, time: number, easingFn: EaseFn | string, isSlient?: boolean): void;
    private animate;
    stop(): void;
}
